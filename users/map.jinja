# -*- coding: utf-8 -*-
# vim: ft=jinja


{##
Setup variables using grains['os_family'] based logic, only add key:values
that differ from what is in defaults.yaml
##}
{% set users = salt['grains.filter_by']({
        'Ubuntu': {
          'user_add': 'canonical',
	  'user_rm': 'redhat',
          'key_path': 'tests/ubuntu.pem'},
        'CentOS': {
          'user_add': 'redhat',
          'user_rm': 'canonical',
          'key_path': 'tests/centos.pem'}
  }
  , grain="os_family"
  , merge=salt['pillar.get']('users:users'))
%}
